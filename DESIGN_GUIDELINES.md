# TextCorrection 設計指南與運行規範

## 設計風格

### 整體視覺風格

- **配色方案**：
  - 主色調：深藍色 (`#304FFE`) 和深紫色 (`#5E35B1`)
  - 背景色：深灰色 (`#1E1E1E`) 至黑色 (`#121212`)，適合深色模式
  - 高亮色：綠色 (`#00B050`) 用於新增內容，紅色 (`#FF0000`) 用於刪除內容
  - 文字顏色：白色 (`#FFFFFF`) 或淺灰色 (`#E0E0E0`)，確保在深色背景上有足夠對比度

- **半透明效果**：
  - 使用 `NSVisualEffectView` 或 SwiftUI 的 `.background(.ultraThinMaterial)` 實現毛玻璃效果
  - 元素透明度：主要容器 0.8-0.9，次要元素 0.7-0.8

- **圓角設計**：
  - 主容器圓角：20pt
  - 按鈕和小元素圓角：12pt
  - 確保視覺一致性

### 字體規範

- **主要字體**：粉圓體 (jf-openhuninn)
  - 正文大小：22pt
  - 標題大小：28pt
  - 小型文字/標籤：13-14pt
  - 確保使用 `getPungyuFont()` 方法獲取字體，提供合適的回退機制

- **字重使用**：
  - 常規內容：Regular 或 Medium
  - 強調內容：SemiBold
  - 標題：Bold

### 元件設計

- **按鈕**：
  - 使用漸變背景 (深灰藍色至深灰色)
  - 內部填充：垂直15pt，水平20pt
  - 包含適當的圖標和文字組合
  - 懸停效果：輕微亮度增加

- **文本容器**：
  - 圓角矩形設計
  - 適當內邊距 (15pt)
  - 輕微陰影效果
  - 文本差異高亮顯示

- **狀態欄圖標**：
  - 簡潔設計，大小合適 (18x18pt)
  - 支持淺色/深色模式自動適配

### 動畫與過渡效果

- **統一動畫時長**：
  - 標準過渡：0.3秒
  - 淡入淡出：0.2-0.3秒
  - 內容加載：0.4秒
  - 使用 `NSAnimationContext` 或 SwiftUI 的 `.animation(.easeInOut)`

- **窗口大小調整**：
  - 平滑動畫效果
  - 持續時間：0.4秒
  - 保持窗口頂部位置不變

- **反饋動畫**：
  - 操作成功動畫：圖標淡入淡出
  - 複製操作：顯示確認動畫

## 運行機制

### 應用啟動流程

1. **應用初始化**：
   - `AppDelegate` 初始化核心服務
   - 加載用戶設定
   - 初始化狀態欄圖標

2. **服務準備**：
   - `StatusItemManager` 設置狀態欄圖標和菜單
   - `OpenAIService` 準備 API 連接
   - `HotKeyManager` 註冊全局快捷鍵

3. **用戶交互準備**：
   - 監聽系統剪貼板變化
   - 設置菜單項回調
   - 準備文本處理窗口

### 文本處理流程

1. **用戶輸入**：
   - 透過粘貼、拖放或快捷鍵獲取文本

2. **文本處理**：
   - `TextWindowManager` 創建並顯示處理窗口
   - `AppDelegate` 調用 `rewriteText()` 方法開始處理
   - `OpenAIService` 發送API請求並開始接收流式回應

3. **文本顯示**：
   - `TextProcessing` 處理文本差異
   - `TextWindowManager` 實時更新顯示
   - 窗口大小自動調整以適應內容

4. **操作完成**：
   - API 處理完成後顯示統計信息
   - 用戶可以選擇性複製文本
   - 紅色標記（刪除文本）在複製時會被略過

### 用戶交互模式

1. **狀態欄操作**：
   - 點擊狀態欄圖標顯示菜單
   - 菜單包含主要功能和設置入口

2. **快捷鍵操作**：
   - 使用全局快捷鍵觸發文本處理
   - 支持文本窗口內的複製快捷鍵

3. **窗口操作**：
   - 支持窗口移動、調整大小
   - 窗口保持在最前層顯示

4. **文本選擇與複製**：
   - 支持選擇性文本複製
   - 自動忽略紅色（被刪除）文本
   - 提供複製成功的視覺反饋

### API 與網絡

1. **API 調用流程**：
   - 從 Keychain 獲取 API 密鑰
   - 構建請求，設置流式響應
   - 處理響應串流並實時更新 UI

2. **錯誤處理**：
   - 網絡連接問題處理
   - API 限制與錯誤處理
   - 用戶友好的錯誤提示

3. **離線功能**：
   - 支持基本的離線文本處理
   - 適當緩存以減少 API 調用

## 技術實現細節

### SwiftUI 與 AppKit 整合

- **混合架構**：
  - 新功能優先使用 SwiftUI 實現
  - 核心功能仍使用 AppKit
  - 使用橋接技術整合兩者

- **橋接方式**：
  - 使用 `NSViewRepresentable` 將 AppKit 視圖引入 SwiftUI
  - 使用 `NSHostingController` 在 AppKit 中使用 SwiftUI 視圖

### 並發處理

- **任務隔離**：
  - 使用 `@MainActor` 標記 UI 相關代碼
  - 網絡請求在後台執行
  - 確保數據訪問安全

- **異步操作**：
  - 使用 Swift 的新並發模型
  - 適當處理 UI 更新
  - 確保 `Sendable` 類型安全

### 性能優化

- **文本處理**：
  - 大型文本分段處理
  - 使用高效的差異算法
  - 避免頻繁的 UI 重繪

- **內存管理**：
  - 適當使用 `weak` 引用避免循環依賴
  - 處理大型文本時注意內存使用

## 測試與品質保證

- **單元測試**：
  - 核心功能需要單元測試覆蓋
  - 使用 XCTest 框架

- **UI 測試**：
  - 關鍵用戶流程測試
  - 使用 XCUITest 進行 UI 自動化測試

- **手動測試清單**：
  - 功能測試：各項功能是否按預期工作
  - 視覺測試：UI 是否符合設計規範
  - 性能測試：各項操作是否流暢

## 附錄：設計資源

- **字體資源**：粉圓體字體文件位於 `TextCorrection/粉圓體.otf`
- **圖標資源**：使用 SF Symbols 系統圖標
- **示例設計**：
  - 文本窗口
  - 狀態欄菜單
  - 設置界面 